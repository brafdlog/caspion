export const displayName = 'YNAB';
export const description = 'Update your YNAB account with the transactions';

export const output = async (transactions, config) => {
  console.log(transactions, config);
};
